# Set this in Vercel's Project Settings -> Environment Variables (not committed to git)
# Point it at your deployed Render backend, including the /api/v1 suffix
VITE_API_URL=https://your-backend.onrender.com/api/v1